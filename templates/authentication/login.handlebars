<!DOCTYPE html>
<html>
<head>
    <title>Login Form</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.17/dist/tailwind.min.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            background-color: #f2f2f2;
        }
    </style>
</head>
<body>
    <div class="container mx-auto px-4">
        <div class="max-w-md mx-auto bg-white rounded-lg shadow-lg p-6">
            <h2 class="text-center mb-6 text-2xl font-bold">Login</h2>
            <form action="/login" method="post">
                <div class="mb-4">
                    <label for="email" class="block mb-2 text-sm font-medium text-gray-600">Email</label>
                    <input type="text" name="email" id="email" class="border border-gray-300 rounded px-3 py-2 w-full">
                </div>
                <div class="mb-4">
                    <label for="password" class="block mb-2 text-sm font-medium text-gray-600">Password</label>
                    <input type="password" name="password" id="password" class="border border-gray-300 rounded px-3 py-2 w-full">
                </div>
                <div class="mb-4 flex items-center">
                    <input type="radio" name="user_type" value="student" id="student" class="mr-2">
                    <label for="student" class="text-gray-600">Student</label>
                    <input type="radio" name="user_type" value="doctor" id="doctor" class="mr-2">
                    <label for="doctor" class="text-gray-600">Doctor</label>
                    <input type="radio" name="user_type" value="admin" id="admin" class="mr-2">
                    <label for="admin" class="text-gray-600">Admin</label>
                </div>
                <div>
                    <button type="submit" class="w-full bg-green-500 text-white rounded py-2 px-4">Login</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>
